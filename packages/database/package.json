{
  "name": "database",
  "scripts": {
    "db:generate": "prisma generate",
    "db:seed": "ts-node prisma/seed.ts",
    "db:push": "prisma db push --skip-generate",
    "dev": "prisma studio"
  },
  "main": "./index.ts",
  "types": "./index.ts",
  "dependencies": {
    "@prisma/client": "^4.8.1"
  },
  "devDependencies": {
    "prisma": "^4.8.1"
  }
}